{программа получает на вход натуральные 
числа n; a-цифра, поторяющаяся в числе; k - число, с которым сравнивают a.
необходимо определить сколько раз цифра a встречается в числе n и верно ли, что цифра a встречается в нём более k раз.}

var n,a,k,i:integer;
begin
  i:=0;
  readln(n,a,k);
  while (n<>0) do {испр. 1)т.к. если будет (n=0), то мы не зайдем внутрь цикла. 2) после ключевого слова цикла while необходимо do}
  begin
    if (a=n mod 10) then 
    begin
      inc(i);
    end;
    n:=n div 10;
  end;
  writeln(i);
  if i>k then 
    writeln('YES')
  else 
    writeln('NO')
end.
