{дано натуральное число n. Необходимо установить, является ли последовательность его цифр 
при просмотре их справа налево упорядоченной по возрастанию. необходимо вывести ответ "да", 
если последовательность является упорядоченной по возратсанию, в противном случае программа выводит "нет"}
var n,a,flag:integer;
begin
  readln(n);
  a:= n div 10;
  flag:=0;
  while (a<>0) do
  begin
    if (a mod 10 <= n mod 10) then {испр. необходимо поставить условие от противного}
    begin
      flag:=1;
     end;
     n:=n div 10;
     a:=a div 10;
  end;
  if flag=0 then {если условие не соблюдается, значит ответ "да"}
    writeln('YES')
  else
    writeln('NO');
end.
